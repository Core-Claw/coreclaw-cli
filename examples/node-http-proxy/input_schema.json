{
  "description": "Node.js HTTP proxy contract Worker",
  "b": "targets",
  "properties": [
    {
      "title": "Target URLs",
      "name": "targets",
      "type": "array",
      "editor": "requestList",
      "description": "HTTP URLs to fetch through the CoreClaw SOCKS5 proxy.",
      "default": [
        {
          "url": "http://example.com/"
        }
      ],
      "required": true
    },
    {
      "title": "Timeout (ms)",
      "name": "timeoutMs",
      "type": "integer",
      "editor": "number",
      "description": "Socket timeout for the proxy connection and HTTP response.",
      "default": 10000,
      "minimum": 1000,
      "maximum": 60000
    }
  ]
}
